<%= link_to "Back", user_profile_path(@user.id), :class => "btn btn-primary" %>

</br>

<%= render('error_user') %>

<h1>Change Password</h1>

<%= form_for(@user, :url => user_profile_change_password_path) do |f| %>

<!-- Partial form with input fields -->
<%= render(:partial=> 'edit_password_form', :locals => {:f => f}) %>

</br></br>

<%= f.submit("Change") %>

<% end %>
