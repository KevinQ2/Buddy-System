<% provide(:title,"Edit profile") %>
<%= link_to "Back", admin_profile_path, class: "btn btn-primary" %>

<%= render('error_admin') %>

<h1>Edit Profile</h1>

<%= form_for(@admin, :url => edit_admin_profile_path) do |f| %>

<!-- Partial form with input fields -->
<%= render(:partial=> 'edit_profile_form', :locals => {:f => f}) %>

</br></br>

<%= f.submit("Update info") %>

<% end %>
